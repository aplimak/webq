[package]
name = "webq-rs"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]
name = "webq_rs"

[dependencies]
uniffi = { version = "0.32.0" }

[build-dependencies]
uniffi = { version = "0.32.0", features = ["build"] }
